import React from 'react';
import Sidebar from '../../Sidebar';

const Setting = () => {
    return (
        <div>
            <Sidebar>
            <h1>Settings</h1>
            </Sidebar>
        </div>
    );
};

export default Setting;